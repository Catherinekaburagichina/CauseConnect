package com.example.causeconnect.ui.theme

import androidx.compose.ui.graphics.vector.ImageVector

data class NavItem(
    val label:String,
    val icon: ImageVector
)
//revisit
//We create nav item data class,then we create list from that then we loop.